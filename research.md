# Research Report: KST 시간대 적용 및 공유하기 기능 분석

## 1. KST (한국 표준시) 시간대 적용 분석
### 현재 상태
- 앱 내부에서 `OffsetDateTime.now()`를 호출하여 Supabase로 전송 중입니다.
- 안드로이드 시스템 설정이 한국어로 되어 있더라도, Supabase 서버나 JVM 기본 설정에 따라 UTC 기준으로 저장되는 경우가 발생할 수 있습니다.

### 해결 방안
- **명시적 ZoneId 사용**: `OffsetDateTime.now(ZoneId.of("Asia/Seoul"))`를 명시적으로 사용하여 서버 환경과 무관하게 항상 한국 시간으로 데이터를 전송합니다.
- **DTO 전송 시 포맷팅**: `ISO_OFFSET_DATE_TIME` 포맷을 유지하되, 오프셋이 `+09:00`인 것을 보장합니다.

## 2. 공유하기(Share) 기능 아이디어: '식물 주민등록증' 컨셉
유저님이 아이디어를 요청하셔서, 다마고치 감성을 극대화할 수 있는 **[식물 주민등록증(ID Card)]** 컨셉을 제안합니다.

### 구성 요소
- **사진**: 사용자가 등록한 식물 사진 (필터 적용 가능)
- **정보**: 
  - `이름(닉네임)` : 크게 표시
  - `출생일(입양일)` : "2024년 3월 8일 우리집에 옴"
  - `MBTI/성격` : (AI 진단 결과 기반) "햇살을 좋아하는 낙천주의자" 등
  - `건강 상태` : 현재의 기분 이모지와 매치 스코어
- **디자인**: 실제 카드 모양의 프레임에 캐릭터 씰(Seal)이 찍힌 형태

### 구현 방식
- `ShareCard` 전용 Composable을 작성하여 렌더링하고, 이를 `Picture` 또는 `GraphicLayer`를 이용해 Bitmap으로 캡처하여 시스템 공유 시트로 전달합니다.
